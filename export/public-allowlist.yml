version: 1

# Paths/globs that are allowed to be exported from the private repo.
# Keep this allowlist intentionally small and explicit.
include:
  - README.md
  - LICENSE
  - CHANGELOG.md
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src-ts/**
  - dist/**
  - schema/**
  - schemas/**
  - examples/**

# Tracked files that must exist in the export output.
required:
  - README.md
  - LICENSE
  - package.json
  - src-ts/cli.ts
